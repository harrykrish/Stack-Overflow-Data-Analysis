{"title": "Changing input loop condition skips half of input values", "tags": ["java", "loops"], "creation_date": 1477162479, "score": 1, "body": "<p>Assuming you literally made the precise change you mentioned, your loop would end up looking like:</p>\n\n<pre><code>while ((s.hasNextInt()) &amp;&amp; (s.nextInt()) &gt;= 0){\n    int key = s.nextInt();\n    st.put(key, i);\n    i++;\n}\n</code></pre>\n\n<p>Which calls <code>nextInt()</code> twice per iteration, which of course skips every other value, as <code>nextInt()</code> consumes the input.</p>\n\n<p>One typical approach here is to declare <code>key</code> outside the loop so you have it available in the condition's scope, then assign and test it all in one go, like:</p>\n\n<pre><code>int key;\nwhile ((s.hasNextInt()) &amp;&amp; (key = s.nextInt()) &gt;= 0){ // &lt;- key is assigned *and* tested\n    st.put(key, i);\n    i++;\n}\n</code></pre>\n\n<p>Thus, one <code>nextInt()</code> per iteration.</p>\n", "last_activity_date": 1477163487, "answer_id": 40195848, "is_accepted": true, "owner": {"user_id": 616460, "reputation": 23170, "user_type": "registered", "accept_rate": 96, "display_name": "Jason C", "link": "http://stackoverflow.com/users/616460/jason-c", "profile_image": "https://i.stack.imgur.com/Yiloe.png?s=128&g=1"}, "last_edit_date": 1477163487, "question_id": 40195790}